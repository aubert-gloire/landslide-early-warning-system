# Backend runtime
fastapi>=0.111.0
bcrypt>=4.0.0
uvicorn[standard]>=0.29.0
motor>=3.3.2
pydantic>=2.7.1
pydantic-settings>=2.2.1
apscheduler>=3.10.4
africastalking>=1.2.7
httpx>=0.27.0
python-dotenv>=1.0.1
python-multipart>=0.0.9

# ML / data — versions pinned to match training environment
# sklearn 1.9.0 renamed SimpleImputer._fill_dtype → _fit_dtype, breaking joblib deserialization
scikit-learn==1.7.2
xgboost>=3.0.0,<4.0.0
imbalanced-learn>=0.12.0,<0.15.0
pandas>=2.1.0
numpy>=1.26.0
joblib>=1.4.0
pyarrow>=15.0.0

# Geo
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
rasterio>=1.3.0
h5py>=3.10.0

# Data pipeline (daily/live path only — GPM IMERG + CHIRPS rainfall)
requests>=2.31.0
pymongo>=4.6.0

# NOTE: pysheds and earthengine-api are intentionally NOT installed here.
# They're only used by one-time setup scripts (scripts/setup_db.py — DEM,
# slope units, NDVI), never by the deployed API or daily pipeline. Keeping
# them out of this file reduces the Render web service's memory footprint
# on a resource-constrained plan. See scripts/requirements-setup.txt.
